import { Config } from "../types";
declare const readConfigFromFile: (config: Config) => Config;
declare const readConfigFromEnv: (config: Config) => Config;
declare const validateConfig: (config: Config) => void;
export { readConfigFromFile, readConfigFromEnv, validateConfig };
