import { Config, BuildResponse, TestRunMultipart, TestRunBase64, TestRunBuffer } from "./types";
import TestRunResult from "./testRunResult";
export declare class VisualRegressionTracker {
    private config;
    private buildId;
    private projectId;
    private axiosConfig;
    constructor(explicitConfig?: Config);
    private isStarted;
    start(): Promise<BuildResponse>;
    stop(): Promise<void>;
    private submitTestRunBase64;
    private submitTestRunMultipart;
    private handleResponse;
    private handleException;
    private getFormData;
    /**
     * Submit test data to external VRT service
     *
     * @param test Test data to track
     * @param retryCount Retry count, default is 2
     * @returns
     */
    track(test: TestRunBase64 | TestRunMultipart | TestRunBuffer, retryCount?: number): Promise<TestRunResult>;
}
